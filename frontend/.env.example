# Default backend address baked into the build.
#
# Only a default: the app can be re-pointed at runtime from the connect screen
# or Profile -> Server, so a new WiFi or tunnel URL does not need a rebuild.
#
# Find your laptop's LAN address with:  ipconfig getifaddr en0
REACT_APP_API_URL=http://192.168.1.5:8001/api
